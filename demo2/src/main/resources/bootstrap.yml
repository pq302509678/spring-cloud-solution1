server:
  port: 8092

spring:
  application:
    name: demo2
  cloud:
    nacos:
      # 分布式配置
      config:
        # Data ID（如果为空，则默认为spring.application.name）
        prefix: com.wanglibing.demo2
        # 配置格式
        file-extension: yaml
        # Nacos Server
        server-addr: 192.168.0.201:8848
#        refresh:
#          # 是否开启动态刷新
#          enable: true

